<%# テキストメール版。HTMLテンプレートと合わせてマルチパート送信する %>
<%# マルチパートにする理由:
    - HTML表示が制限されたクライアント向けにフォールバックを提供するため
    - HTML版と同じ情報をtextでも提供して可読性・互換性を担保するため
%>
<%= @reservation.name %> 様

ご予約いただいた上映が明日に迫っております。以下の内容をご確認ください。

<%= render 'reservation_mailer/reservation_summary' %>

ご来場を心よりお待ちしております。
