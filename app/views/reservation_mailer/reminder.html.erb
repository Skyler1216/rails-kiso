<%# HTMLメール版。textテンプレートと組み合わせてマルチパート送信する %>
<%# マルチパートにする理由:
    - 受信環境に依存せず確実に内容を届けるため（HTML不可ならtextを使用）
    - スパム判定やセキュリティポリシーの影響を低減するため
    - アクセシビリティと互換性を高めるため
%>
<p><%= @reservation.name %> 様</p>

<p>ご予約いただいた上映が明日に迫っております。以下の内容をご確認ください。</p>

<%= render 'reservation_mailer/reservation_summary' %>

<p>ご来場を心よりお待ちしております。</p>
