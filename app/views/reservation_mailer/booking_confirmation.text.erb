<%# テキストメール版。HTMLテンプレートと合わせてマルチパート送信する %>
<%# マルチパートにする理由:
    - HTMLが無効な環境でも本文が正しく読めるようにするため
    - HTML版と同等の情報をtextでも提供し、可用性・到達性を高めるため
    - 一部のクライアントやセキュリティ設定でHTMLが除去されても内容を確実に伝えるため
%>
<%= @reservation.name %> 様

この度はご予約ありがとうございます。以下の内容で予約が完了しました。

<%= render 'reservation_mailer/reservation_summary' %>

ご来場を心よりお待ちしております。
