/**
 * ========================================
 * 🎬 映画館サイト Tailwind CSS 設定ファイル
 * ========================================
 * 
 * このファイルは映画館サイト専用のTailwind CSS設定を定義します。
 * カスタムカラー、フォント、シャドウなどを設定できます。
 * 
 * 設定変更後は以下を実行してください:
 * npm run tailwind:build または npm run tailwind:watch
 */

export default {
  // ========================================
  // 📁 コンテンツ検索パス
  // ========================================
  // Tailwind CSSが監視するファイルパス
  // これらのファイルで使用されているクラスのみがビルド出力に含まれます
  content: [
    "./app/views/**/*.html.erb",      // Railsビューファイル（HTMLテンプレート）
    "./app/helpers/**/*.rb",          // Railsヘルパーファイル（Ruby）
    "./app/javascript/**/*.js",      // JavaScriptファイル
    "./app/assets/stylesheets/**/*.css" // CSSファイル
  ],

  // ========================================
  // 🎨 テーマ設定
  // ========================================
  theme: {
    extend: {
      // ========================================
      // 🌈 カスタムカラー
      // ========================================
      // 映画館らしいブランドカラーを定義
      colors: {
        brand: {
          night: "#0b0d17",    // 🌙 映画館の暗い夜の色（背景用）
          red: "#e50914",      // 🔴 Netflix風の鮮やかなレッド（アクション用）
          gold: "#f9b234",     // 🟡 高級感のあるゴールド（アクセント用）
          slate: "#1d2635"     // 🔘 落ち着いたスレート（補助色用）
        }
      },

      // ========================================
      // 🔤 カスタムフォント
      // ========================================
      // 映画館らしいフォントファミリーを定義
      fontFamily: {
        sans: ["'Noto Sans JP'", "'Segoe UI'", "sans-serif"],  // 📝 本文用フォント（日本語対応）
        heading: ["'Roboto Slab'", "serif"]                    // 📰 見出し用フォント（高級感）
      },

      // ========================================
      // ✨ カスタムシャドウ
      // ========================================
      // 映画館らしい光る影効果を定義
      boxShadow: {
        glow: "0 10px 25px -15px rgba(249, 178, 52, 0.45)"  // ✨ ゴールドの光る影
      }
    }
  },

  // ========================================
  // 🔌 プラグイン
  // ========================================
  // Tailwind CSSの機能を拡張するプラグイン
  plugins: [
    (await import("@tailwindcss/forms")).default  // 📝 フォーム要素のスタイリング改善
  ]
};
